<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login IN</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900">
    <div class="p-4">
      <% if (locals.error) { %>
      <h1 class="text-3xl text-white font-bold uppercase mb-4"><%= error %></h1>
      <a
          class="px-5 py-2 bg-red-600 rounded-lg text-white mt-10"
          href="/login"
        >
          Try Again
        </a>
      <% } else { %>
      <h1 class="text-4xl text-white font-bold uppercase">Login IN</h1>

      <form action="/user/login" method="post" class="mt-3">
        <input
          type="email"
          placeholder="Enter Your Email"
          name="email"
          required
          class="w-[330px] px-3 py-2 rounded-lg text-white bg-zinc-700 focus:outline-none block mt-3"
        />

        <input
          type="password"
          placeholder="Enter Your Password"
          name="password"
          required
          class="w-[330px] px-3 py-2 rounded-lg text-white bg-zinc-700 focus:outline-none block mt-3"
        />

        <button
          type="submit"
          class="px-5 py-2 bg-green-500 rounded-lg text-white mt-2 mt-3"
        >
          Login
        </button>
      </form>

      <% } %>
    </div>
  </body>
</html>
